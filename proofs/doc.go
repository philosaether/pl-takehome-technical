// Package proofs holds the M2 deterministic proofs (ordering-under-crash, gate,
// flush, look-ahead cost). M0 placeholder — see roadmap.md M2. The M0 smoke
// proofs (in-order drain, gate) live in internal/queue/worker_test.go against the
// in-memory oracle.
package proofs
